# Intentionally empty.
#
# This site is served by Cloudflare Workers static assets, which already provide
# clean URLs natively: /privacy.html auto-redirects to /privacy, and /privacy
# serves privacy.html. Adding "/privacy -> /privacy.html" rules here (a Cloudflare
# Pages pattern) fights that behaviour and causes an infinite redirect loop, which
# is why the privacy and terms pages failed to load. Leave this file rule-free.
